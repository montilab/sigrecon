% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netProp.R
\name{netProp}
\alias{netProp}
\title{Network-propagation based Recontextualization, from expression data to signature.}
\usage{
netProp(
  se = NULL,
  seeds,
  ig = NULL,
  sig = c("rwr", "corr"),
  avg_p = FALSE,
  avg_p_vals = c(1e-04, 0.1),
  avg_p_length = 5,
  p = 0.1,
  bootstrap = FALSE,
  n_bootstraps = 1000,
  limit = 30,
  assay_name = NULL,
  nfeatures = NULL,
  min.sft = 0.85,
  beta = NULL,
  cores = 1,
  cor.fn = c("bicor", "cor"),
  cor.type = c("unsigned", "signed hybrid", "signed"),
  powers = c(seq(1, 10, by = 1), seq(12, 20, by = 2)),
  diag_zero = TRUE
)
}
\arguments{
\item{se}{A SummarizedExperiment object containing target-context expression
data. Not needed if \code{ig} is supplied.}

\item{seeds}{Either a single unnamed gene "TP53", a named list of genes, or
a list of named lists of genes.}

\item{ig}{An optional pre-built igraph, e.g. from a previous \code{\link[=wgcna.adj]{wgcna.adj()}}
call. If supplied, network construction is skipped and \code{se} is ignored.}

\item{sig}{A string specifying the type of network signature: random walk, correlation etc.}

\item{avg_p}{A boolean specifying whether to ensemble random walk results over a range of restart values}

\item{avg_p_vals}{A numeric vector specifying the start and end of a arithmetic sequence to explore restart values.}

\item{avg_p_length}{A numeric specifying how many values within \code{avg_p_vals} to include in the ensemble}

\item{p}{A numeric specifying the restart value for random walk, default=0.1}

\item{bootstrap}{A boolean specifying whether to use empirical distributions of stationary values to find significant genes.}

\item{n_bootstraps}{A numeric specifying the number of bootstraps to perform.}

\item{limit}{A numeric specifying the number of genes to be included in the network signature. Default is 30.}

\item{assay_name}{Assay name in \code{se} to use for network construction. Defaults to the first assay.}

\item{nfeatures}{Number of variable genes to use when learning the graph from \code{se}.
Defaults to \code{min(10000, nrow(assay(se)))}.}

\item{min.sft}{Minimum scale-free topology fitting index used by \code{\link[=wgcna.adj]{wgcna.adj()}}.}

\item{beta}{Optional soft-thresholding power passed to \code{\link[=wgcna.adj]{wgcna.adj()}}.}

\item{cores}{Number of CPU cores passed to \code{\link[=wgcna.adj]{wgcna.adj()}}.}

\item{cor.fn}{Correlation function passed to \code{\link[=wgcna.adj]{wgcna.adj()}}.}

\item{cor.type}{Correlation network type passed to \code{\link[=wgcna.adj]{wgcna.adj()}}.}

\item{powers}{Candidate power values passed to \code{\link[=wgcna.adj]{wgcna.adj()}} when \code{beta} is \code{NULL}.}

\item{diag_zero}{Whether to zero the diagonal of the learned adjacency matrix
before converting it to igraph.}
}
\value{
A named list of recontextualized gene signatures.
}
\description{
Learns a gene co-expression network from target-context expression data
with \code{\link[=wgcna.adj]{wgcna.adj()}}, then propagates seed signatures across it with
\code{\link[=network_sig]{network_sig()}}. This is the one-call convenience wrapper for the two-step
\code{wgcna.adj()} + \code{network_sig()} workflow; use \code{wgcna.adj()}/\code{network_sig()}
directly (or pass a pre-built \code{ig}) when you need to reuse the same
network across many propagation calls, since building the network is the
expensive step.
}
