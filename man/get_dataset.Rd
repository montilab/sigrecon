% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{get_dataset}
\alias{get_dataset}
\title{Fetch a bundled perturbational signature dataset}
\usage{
get_dataset(name, force = FALSE)
}
\arguments{
\item{name}{Dataset name. See \code{\link[=list_datasets]{list_datasets()}} for available names.}

\item{force}{Logical. If \code{TRUE}, re-download even if a cached copy exists.
Default is \code{FALSE}.}
}
\value{
A named list of genesets. Each element has an \code{up} component
(top 100 DEGs) and an \code{up_full} component (the full ranked gene list).
}
\description{
Downloads (and locally caches, via \code{BiocFileCache}) one of the datasets
listed in \code{\link[=list_datasets]{list_datasets()}}. Datasets are hosted as release assets on
\code{montilab/sigrecon} rather than bundled with the package, so the first
call to \code{get_dataset()} for a given name requires network access;
subsequent calls reuse the local cache.
}
\examples{
\dontrun{
tahoe_nci_h23 <- get_dataset("tahoe.nci_h23")
}

}
