% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseline_methods.R
\name{recontextualize}
\alias{recontextualize}
\title{Recontextualize signatures with a selected baseline method}
\usage{
recontextualize(
  method = c("networkProp", "projectCor", "mean"),
  se = NULL,
  seeds = NULL,
  sigs = NULL,
  score = c("gsva", "AUCell", "eigen"),
  sig = c("rwr", "corr"),
  avg_p = FALSE,
  avg_p_vals = c(1e-04, 0.1),
  avg_p_length = 5,
  p = 0.1,
  bootstrap = FALSE,
  n_bootstraps = 1000,
  limit = NULL,
  nfeatures = NULL,
  min.sft = 0.85,
  beta = NULL,
  cores = 1,
  cor.fn = c("bicor", "cor"),
  cor.type = c("unsigned", "signed hybrid", "signed"),
  powers = c(seq(1, 10, by = 1), seq(12, 20, by = 2)),
  diag_zero = TRUE,
  perturbation_col = NULL,
  condition_col = NULL,
  perturbed_label = "Perturbed",
  control_label = "Control",
  design_vars = NULL,
  assay_name = NULL,
  alpha = 0.05,
  min_count = NULL,
  min_samples = 1
)
}
\arguments{
\item{method}{Baseline recontextualization method. One of \code{"networkProp"},
\code{"projectCor"}, or \code{"mean"}.}

\item{se}{A SummarizedExperiment object used by \code{"projectCor"}, \code{"mean"},
and to learn the \code{"networkProp"} graph.}

\item{seeds}{Seed signatures for the \code{"networkProp"} method.}

\item{sigs}{Signature list for the \code{"projectCor"} method. For \code{"mean"}, names
are interpreted as perturbation labels and lengths are used as output sizes
when \code{limit} is not supplied.}

\item{score}{Scoring method used by \code{"projectCor"}.}

\item{sig}{Network signature mode passed to \code{\link[=network_sig]{network_sig()}}. Defaults to \code{"rwr"}.}

\item{avg_p}{Whether to ensemble network propagation over multiple restart probabilities.}

\item{avg_p_vals}{Range of restart probabilities used when \code{avg_p = TRUE}.}

\item{avg_p_length}{Number of restart probabilities to average when \code{avg_p = TRUE}.}

\item{p}{Restart probability passed to \code{\link[=network_sig]{network_sig()}}.}

\item{bootstrap}{Whether to use bootstrap-based extraction in \code{\link[=network_sig]{network_sig()}}.}

\item{n_bootstraps}{Number of bootstrap replicates passed to \code{\link[=network_sig]{network_sig()}}.}

\item{limit}{Number of genes to keep for each output signature. If \code{NULL},
\code{"networkProp"} and \code{"projectCor"} keep the original signature lengths and
\code{"mean"} uses the lengths of \code{sigs}.}

\item{nfeatures}{Number of variable genes to use when learning the
\code{"networkProp"} graph from \code{se}. Defaults to \code{min(10000, nrow(assay(se)))}.}

\item{min.sft}{Minimum scale-free topology fitting index used by
\code{\link[=wgcna.adj]{wgcna.adj()}} when learning the \code{"networkProp"} graph.}

\item{beta}{Optional soft-thresholding power passed to \code{\link[=wgcna.adj]{wgcna.adj()}} for
\code{"networkProp"}.}

\item{cores}{Number of CPU cores passed to \code{\link[=wgcna.adj]{wgcna.adj()}} for \code{"networkProp"}.}

\item{cor.fn}{Correlation function passed to \code{\link[=wgcna.adj]{wgcna.adj()}} for \code{"networkProp"}.}

\item{cor.type}{Correlation network type passed to \code{\link[=wgcna.adj]{wgcna.adj()}} for \code{"networkProp"}.}

\item{powers}{Candidate power values passed to \code{\link[=wgcna.adj]{wgcna.adj()}} for \code{"networkProp"}
when \code{beta} is \code{NULL}.}

\item{diag_zero}{Whether to zero the diagonal of the learned \code{"networkProp"}
adjacency matrix before converting it to igraph.}

\item{perturbation_col}{Column in \code{colData(se)} identifying the perturbation
label for each profile. Used by \code{"mean"}.}

\item{condition_col}{Column in \code{colData(se)} indicating whether a profile is
perturbed or control. Used by \code{"mean"}.}

\item{perturbed_label}{Value in \code{condition_col} corresponding to perturbed profiles.}

\item{control_label}{Value in \code{condition_col} corresponding to control profiles.}

\item{design_vars}{Optional character vector of additional covariates to place
before condition in the DESeq2 design formula for \code{"mean"}.}

\item{assay_name}{Assay name in \code{se} to use for \code{"mean"}. Defaults to the first assay.}

\item{alpha}{Adjusted p-value cutoff used by the \code{"mean"} method when selecting
upregulated genes.}

\item{min_count}{Optional count threshold used to filter low-expression genes
before DESeq2 for \code{"mean"}. If \code{NULL}, no pre-filtering is applied.}

\item{min_samples}{Minimum number of samples that must satisfy \code{min_count}
for a gene to be retained when \code{min_count} is provided.}
}
\value{
A named list of recontextualized gene signatures.
}
\description{
Recontextualize signatures with a selected baseline method
}
