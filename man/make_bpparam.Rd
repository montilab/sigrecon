% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util.R
\name{make_bpparam}
\alias{make_bpparam}
\title{Create appropriate BiocParallel backend}
\usage{
make_bpparam(workers = 1, RNGseed = NULL, progress = FALSE, type = NULL)
}
\arguments{
\item{workers}{Number of parallel workers (default: 1 for sequential)}

\item{RNGseed}{Random seed for reproducibility (default: NULL)}

\item{progress}{Show progress bar (default: FALSE)}

\item{type}{Force backend type: "multicore", "snow", or "serial" (default: auto-detect)}
}
\value{
A BiocParallelParam object
}
\description{
Create appropriate BiocParallel backend
}
\examples{
# Sequential (default)
bp <- make_bpparam()

# Parallel on Unix/Mac (forking)
bp <- make_bpparam(workers = 2, RNGseed = 123)

# Parallel on Windows (socket)
bp <- make_bpparam(workers = 2, RNGseed = 123)

# With progress bar
bp <- make_bpparam(workers = 2, progress = TRUE)

}
